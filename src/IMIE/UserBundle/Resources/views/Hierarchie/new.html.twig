{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hierarchie creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('hierarchie') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
